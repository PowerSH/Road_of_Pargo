# 일일 작업 보고서 — 2026-05-26

- **작업 환경**: macOS (Godot 4.6 미설치 → 코드베이스 전용 작업)
- **세션 성격**: 동기화 + 다음 작업 준비

---

## 1. 오늘 한 일

### Windows PC 작업 결과 수령
- `git pull --ff-only` — fast-forward로 1 커밋 수령 (`2ec91dc`)
- 변경 규모: 72 파일, +1513 / -33
- 머지 충돌 없음

### 받은 내용 (Windows에서 작업된 4회차 세션 산출물)
- **Godot 4.3 → 4.6 마이그레이션** + 호환성 fix들
- **11개 UI 씬 + 11개 UI 스크립트** 추가 (`scenes/*.tscn`, `scripts/ui/*.gd`)
- **`map_screen` 실제 구현** (StS DAG, Fog of War, 노드 라우팅)
- **`RunState` Fog of War 데이터**: `revealed_nodes`, `is_revealed`, `reveal_initial`
- **배경 이미지 7장** (`resource/bg_img/`)
- **`docs/session-reports/2026-05-25-godot-4-6-and-ui-skeleton.md`** 추가
- **NEXT-SESSION.md, README.md** 갱신 (Godot 4.6 함정 5개, 씬 흐름 다이어그램 포함)

### 정책 변경 인지
- 기존: "Claude는 씬 안 만듦"
- 갱신(4회차): **"Claude가 placeholder .tscn + .gd 구현, 사용자는 시각 디자인 다듬음"**

### 일일 보고서 폴더 생성
- `docs/daily-reports/` 폴더 신설
- 본 파일 = 첫 임시 샘플

### 배틀 시스템 기획 (대화 라운드 1-6 종합)

**6 차원 모두 풀이 완료**. 결정 → 데이터 모델 → 설계 문서 → 코드 1차 작성 까지 한 번에 진행.

- **라운드 1 (편성 UX)**: 5×3 마주보기, 영구 보드 상태, 보관함 사이드 패널, 시너지 실시간, 같은 유닛 2번 배치 OK
- **라운드 5 (적 시스템)**: EnemyUnitData 별도, 5개 적 시너지 패턴, 카탈로그+변동, power 자동 산정, 적도 시너지
- **라운드 4 (전투 결과)**: 패배=게임오버, HP carry over, 무승부 캡 120초, 골드 power×0.25, 보스 ×2
- **라운드 2 (아이템 사용)**: 전투 직전 일괄 적용, 슬롯 3개, 같은 effect_type max만, 사용=카고 회수
- **라운드 3 (연출)**: 별도 페이즈, 2배속까지, 일시정지 OK, HP바·데미지 = Settings, 슬로우모션 결과, 도주 X
- **라운드 6 (데이터 모델)**: OwnedUnit/BattleResult/ItemEffect/Settings 신규 + 기존 마이그레이션 청사진

**신규 클래스 (8개 — 적 시스템 4 + 배틀 시스템 4)** (`scripts/data/` + `scripts/globals/`)
- `enemy_unit_data.gd`, `enemy_synergy_rule.gd`, `encounter_slot.gd`, `encounter_template.gd`
- `owned_unit.gd`, `battle_result.gd`, `item_effect.gd`
- `settings.gd` (autoload, `project.godot` 등록)

**신규/갱신 문서**
- `docs/design/encounters.md` (신규) — 적 시스템 카탈로그
- `docs/design/battle-flow.md` (신규) — 라운드 1-6 통합 (상태머신, UI 룰, 데이터 인터페이스)
- `docs/design/cargo-and-mortality.md` §5 (갱신) — HP carry over + 회복 룰
- `docs/NEXT-SESSION.md` — 합의 표에 R1-R6 결정 12줄 추가, 읽기 순서·코드 위치 갱신

## 2. 오늘 직접 변경한 파일

| 파일 | 변경 내용 |
|---|---|
| `docs/daily-reports/2026-05-26.md` | (신규) 본 보고서 |
| `docs/NEXT-SESSION.md` | §6 일일 보고서 운영 + 합의 표 12줄 추가 + 읽기 순서·코드 위치 갱신 |
| `scripts/data/enemy_unit_data.gd` | (신규) 적 유닛 데이터 클래스 |
| `scripts/data/enemy_synergy_rule.gd` | (신규) 적 시너지 룰 (5 EffectType) |
| `scripts/data/encounter_slot.gd` | (신규) 인카운터 슬롯 |
| `scripts/data/encounter_template.gd` | (신규) 인카운터 템플릿 + power 산정 |
| `scripts/data/owned_unit.gd` | (신규) 플레이어 영입 유닛 인스턴스 (status + carry-over HP) |
| `scripts/data/battle_result.gd` | (신규) 전투 결과 패키지 (Outcome enum, factory) |
| `scripts/data/item_effect.gd` | (신규) 아이템 효과 (5 EffectType) |
| `scripts/globals/settings.gd` | (신규) Settings autoload (HP바·데미지·배속) |
| `project.godot` | Settings autoload 등록 |
| `docs/design/encounters.md` | (신규) 적 시스템 설계 + mock 데이터 |
| `docs/design/battle-flow.md` | (신규) 라운드 1-6 통합 — 상태머신, UI, 데이터 인터페이스 |
| `docs/design/cargo-and-mortality.md` | §5 HP carry over + 회복 룰 갱신 |

## 3. 결정 / 합의 사항

### 배틀 시스템 기획 라운드 1-6 (총 합의)

상세 표는 `docs/NEXT-SESSION.md` 합의 표 / `docs/design/battle-flow.md` §0 참조.

**R1 편성 UX** — 5×3 마주보기, 보드 영구, 보관함 사이드 패널 토글, 시너지 실시간, 같은 유닛 두 번 OK, 사망 버리기 쉼터 한정

**R5 적 시스템** — EnemyUnitData 별도, 5개 적 시너지 패턴 모두 채택 (GLOBAL_TRAIT/BOSS_AURA/DEATH_TRIGGER/HP_THRESHOLD/NUMBER_ADVANTAGE), 카탈로그+변동, power 자동, 적도 시너지 받음. 실제 진영·유닛 카탈로그는 추후 전달 → mock 사용

**R4 전투 결과** — 패배=게임오버, HP carry over (유닛 영구 자원), 무승부 캡 120초, 골드=`power×0.25`, 보스 ×2, 쉼터 풀 회복, 부상 회복 100% HP

**R2 아이템 사용** — 전투 직전 일괄 적용, 슬롯 3개, 같은 effect_type max만, 사용=카고 즉시 회수, 적 시너지 비표시, 취소 자유

**R3 연출** — 별도 페이즈, 1×/2× 배속, 일시정지 OK, HP바·데미지숫자=Settings, 페이드 사망, 시너지 아이콘 항상 노출, 도주 X, 결과 슬로우모션

**R6 데이터 모델** — OwnedUnit/BattleResult/ItemEffect/Settings 1차 작성 완료, 기존 클래스 마이그레이션 청사진은 battle-flow.md §6

### 일일 보고서 운영 규칙 (확정):
  - 위치: `docs/daily-reports/YYYY-MM-DD.md`
  - 트리거: 사용자가 "오늘 작업 정리해" 류의 트리거를 주면 작성
  - 성격: 그 날 무엇을 했는지의 **타임라인성 로그**. 디자인 결정 같은 큰 단위는 기존 `docs/session-reports/`에 별도 보관
  - 구조: 6섹션 (오늘 한 일 / 변경 파일 / 결정 / 환경 메모 / 다음 작업 후보 / 미해결)
  - 파일명: `YYYY-MM-DD.md` (토픽 suffix 없음)
  - 커밋 정책: **그날 작업 커밋과 동시 커밋** (별도 커밋 만들지 말 것)

## 4. 현재 환경 메모

- macOS, Godot 미설치 → **에디터 검증 불가**
- 가능한 작업: 코드 작성, 데이터 모델 설계, 헤드리스 단위 테스트, 디자인 문서
- 불가능: 씬 GUI 조작, 인스펙터 작업, 실행 확인 (Windows PC로 검증 필요)

## 5. 다음 작업 후보 (코드베이스만으로 가능)

배틀 시스템 기획 남은 차원 (대화 라운드로 진행):
- **4번** — 전투 결과 처리 (승/패/무, 부상→사망 판정, 보상)
- **2번** — 사전 아이템 사용 페이즈 (적군 본 후, 시너지/스탯 부스트)
- **3번** — 전투 페이즈 연출 (카메라, 비주얼, 배속, 일시정지)
- **6번** — 데이터 모델 종합 (battle_screen 상태머신, BattleResult 구조)

기획 끝나면 macOS 가능 작업:
1. **`EncounterGenerator`** 구현 — `EncounterTemplate` 받아 enemy_board 생성. 순수 코드.
2. **`EnemySynergyEngine`** — pre-battle 평가만 우선. `SynergyEngine` 패턴 재사용.
3. **적재/사망 시스템 코드** — `OwnedUnit` 클래스 + `RunState.owned_units` 마이그레이션.
4. **챕터 진행 로직** — `RunState.chapter` 필드 추가, 보스 승리 처리.

## 6. 미해결 / 메모

- 일일 보고서 운영 규칙 확정 → `docs/NEXT-SESSION.md`에 반영, 메모리 저장 (커밋 `a5a18c6` 완료).
- 적 시스템 mock 데이터 → 사용자 진영·유닛 카탈로그 도착 시 `.tres`로 교체.
- 배틀 시스템 기획 6 라운드 모두 완료. 데이터 모델 1차 작성됨.
- 다음 코드 작업: `RunState.owned_units` 마이그레이션 + `BoardState` 셀 타입 변경 + `CombatUnit.setup()` HP carry over 적용 + `CombatManager` 전투 종료 시 OwnedUnit 기록.
- `EncounterGenerator` / `EnemySynergyEngine` 후속 구현 — battle-flow.md §6에 청사진.
- battle_screen.tscn 페이즈 컨테이너 구조는 Windows PC에서 작업 (씬 편집).
- `battle-flow.md` §9 TBD 8개 항목.
- `Settings`의 디스크 저장은 후순위 (현재 메모리만).
